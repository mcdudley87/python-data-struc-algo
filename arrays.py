"""
array stores references to values stored elswhere in storage/memory.
stores "array of memories/addresses"
"""
new_list = [1, 2, 3] 
result = new_list[0]
"""
navigating arrays demonstration
"""
if 1 in new_list: print(True)
    
for n in new_list:
    if n == 1:
        print(True)
        
        break